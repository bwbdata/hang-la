# Task Plan: 夯拉排名桌面端 H5

## Goal
规划一个适合电脑端操作的“夯拉排名”图片排序 H5 页面，并明确基于 Vue 3、TypeScript、Vite 的前端架构、模块边界和实施路径。

## Current Phase
Phase 3

## Phases

### Phase 1: 需求与项目现状确认
- [x] 明确目标为电脑端操作的排名 H5 页面
- [x] 确认当前工作目录尚未包含已有应用代码
- [x] 明确图片上传、标题栏、等级网格与无权限需求
- [x] 定义固定五档及用户可选的带标签模板库
- [x] 明确“选择标签类型 → 上传图片 → 拖动排名 → 导出图片”的四步流程
- **Status:** complete

### Phase 2: 信息架构与技术架构
- [x] 设计页面信息架构、核心流程与数据模型
- [x] 规划 Vue 3 + TypeScript + Vite 的目录及模块职责
- [x] 定义状态管理、请求层与权限边界
- **Status:** complete

### Phase 3: 初始化与基础框架
- [x] 初始化 Vite + Vue 3 + TypeScript 工程
- [x] 配置 Pinia、图片本地存储、样式变量、模板常量、四步状态与初始草稿
- [x] 建立步骤条、模板选择器、标题栏、上传区和等级网格公共组件
- **Status:** complete

### Phase 4: 业务页面实现
- [x] 实现图片上传、拖拽排序、跨等级移动与每等级 14 张限制
- [x] 实现 IndexedDB 自动保存、草稿恢复和 PNG 导出
- [x] 完成空态、异常态与删除/清空交互
- **Status:** complete

### Phase 5: 验收与发布
- [ ] 补齐容量、排序与存储的单元测试及桌面端适配验证
- [x] 完成构建与部署产物生成
- **Status:** in_progress

## Key Questions
1. 已确认固定五档：夯、顶级、人上人、NPC、拉完了；仅可修改名称。
2. 上传图片后是否需要待放入区，还是默认放入某一等级？
3. 是否只需导出 PNG，还是还要导入/导出 JSON 草稿？

## Decisions Made
| Decision | Rationale |
|----------|-----------|
| Vue 3 Composition API + TypeScript | 适合复杂表格、筛选和可复用业务逻辑，类型可约束排名数据。 |
| Vite 作为构建工具 | 本地启动和增量构建快速，配置成本低。 |
| Pinia 管理排名草稿 | 集中管理标题、等级、图片顺序、上传待处理图片及保存状态。 |
| IndexedDB 持久化本地图片 | 支持 Blob，刷新页面后仍可恢复上传图片和排序。 |
| CSS Grid 固定 7×2 规则 | 每级 7 列，最多 14 项，布局和容量规则一致。 |
| SortableJS 支持拖拽 | 支持等级内的排序和跨等级转移。 |
| 预设模板库 | 选择器以唯一标签区分各套固定五档名称，完全重复的模板去重。 |

## Errors Encountered
| Error | Attempt | Resolution |
|-------|---------|------------|
| 无现有项目文件 | 1 | 以新项目和可替换 mock 的架构进行规划。 |
| 当前目录不是 Git 仓库，无法执行 `git diff --check` | 1 | 已确认规划文件可读取；后续初始化工程后再接入 Git 校验。 |
| 架构文档补丁上下文过期 | 1 | 读取当前文件后以更小的定位补丁完成更新。 |
